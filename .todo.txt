TODO

0.1 SAVE THE NUMBER OF COLS AND ROWS IN THE DATAVISER CONTEXT TOO
0.2 ADD OPTION TO ZOOM IN ON A PARTICULAR VIZ (it fills up the screen)
0.3 REMOVE VISUAL OPTION
0.4 MAKE SURE TO INDICATE DFS ARE LOADING FOR EACH VIS 
   (each vis should have a loader)

1. Create a composbale class that represents a visualization
   Create a separate class that the first one uses to refernce data stored by redux.

2. Put a loading screen when initializing the app

3. Move the app exit button to the upper right of the screen?